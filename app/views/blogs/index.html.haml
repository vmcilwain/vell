.page-header
  %h1.animated.bounceInDown Articles
  =link_to 'New Article', new_blog_path if user_signed_in? && current_user.admin?
.row
  .col-xs-12
    =will_paginate @blogs, class: 'apple_pagination'
=render @blogs
