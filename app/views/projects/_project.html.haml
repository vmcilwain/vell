-present(project) do |project_presenter|
  =div_for project do
    %h2
      =project_presenter.repo_link
    %p
      =project.description
    %a.btn.btn-primary{href: "#{project_presenter.live_url}", target: :blank}
      Live
      %span.glyphicon.glyphicon-chevron-right
    %hr/
