development:
  adapter: mysql2
  encoding: utf8
  database: vell_development
  pool: 5
  username: root
  password: <%= ENV['DBPASS'] %>
  socket: /tmp/mysql.sock

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  adapter: mysql2
  encoding: utf8
  database: vell_test
  pool: 5
  username: root
  password: <%= ENV['DBPASS'] %>
  socket: /tmp/mysql.sock

# production:
#   adapter: postgressql
#   encoding: unicode
#   database: vextranet_production
#   pool: 5
#   password:

 #Only to precompile assets if you can't get it to work with postgres
production:
  adapter: mysql2
  encoding: utf8
  database: vell_production
  pool: 5
  host: 127.0.0.1
  username: <%= CONFIG[:db_user] %>
  password: <%= CONFIG[:db_pass] %>
  socket: /var/run/mysqld/mysqld.sock
